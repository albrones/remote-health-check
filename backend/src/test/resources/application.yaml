graphql:
  packages:
    - "com.zenika.remotehealthcheck"
spring:
  cloud:
    gcp:
      firestore:
        project-id: remote-health-check-test
        credentials:
          location: classpath:serviceAccountKeyTest.json